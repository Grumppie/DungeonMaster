## HUD Feature

Owns the player-bottom HUD and compact resource display.

Does not own full sheet editing or combat engine data mutation.

Canonical files:
- `PlayerHud.jsx` for shell composition only
- `PlayerBar.jsx` for identity and active-turn summary
- `PartyStatusStrip.jsx` for combat/resource stat presentation
