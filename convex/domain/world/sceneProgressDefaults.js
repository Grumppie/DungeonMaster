export function inferRequiredDiscovery(scene) {
  return scene.requiredDiscovery || "Learn the critical truth in this scene.";
}

export function inferRequiredCommitment(scene) {
  return scene.requiredCommitment || "Take the action that moves the scene forward.";
}
